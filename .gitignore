# Local Claude Code session state (per-machine)
.claude/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Node — only if dependencies ever get added; the app currently has none
node_modules/

# Go build artifact (`go build -o tarock-solve .` in solver/)
solver/tarock-solve
